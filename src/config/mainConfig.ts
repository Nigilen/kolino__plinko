import type { MainConfig } from "@/components/GamePlinko/types";

export const mainConfig: MainConfig = {
  title: 'Plinko',
  modal: {
    title: 'You win',
    bonus: '100',
    button: 'Play again'
  }
}